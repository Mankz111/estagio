name: hello

on:
  push:
    branches:
      - main



jobs:
  job_de_build:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: não_sei_para
      run: encho "Testes feitos a buildar"


jobs:
  test:
  build:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: data
        run: echo "A correr testes"


