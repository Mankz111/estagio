name: hello

on:
  push:
    branches:
      - main



jobs:
  build:
    runs-on: ubuntu-latest


    steps:
    - name: build
      run: echo "Testes feitos a buildar"

  teste:
    runs-on : ubuntu-latest
    needs: build

    steps:
    - name: teste
      run: echo "build"


