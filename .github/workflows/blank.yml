name: hello

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: data
        run: date
      - name: user do commit
        run: echo ${{ github.actor }}
      - name: SHA do commit
        run: echo ${{ github.sha }}
      

