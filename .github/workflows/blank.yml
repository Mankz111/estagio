name: hello

on:
  push:
    branches:
      - main



jobs:
  build:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: build
      run: encho "Testes feitos a buildar"
    - name: test
      run: echo "A correr testes"


