name: hello

on:
  push:
    branches:
      -main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Code check
        uses: actions/checkout@v4

    steps:
      - name: imprimir ola
        run: echo Olá mundo